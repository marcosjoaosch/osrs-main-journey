function dashboardGoalOptions(selected) { return state.goals.map((goal, index) => `<option value="${index}" ${goal.name === selected ? 'selected' : ''}>${escapeTodo(goal.name)}</option>`).join(''); }
function dashboardEditor() { const macro = state.macro?.name, project = state.project?.name, today = state.today?.name; return `<section class="card dashboard-editor"><div class="section-head" style="margin-top:0"><div><span class="eyebrow">PERSONALIZAR PAINEL</span><h2>Escolha o seu 1–1–1</h2></div><button class="ghost-btn" id="closeDashboardEditor" type="button">Fechar</button></div><p class="muted">Escolha metas já cadastradas para aparecerem no seu foco.</p><form class="dashboard-focus-form" id="dashboardFocusForm"><label>Meta macro<select name="macro"><option value="">Selecionar meta...</option>${dashboardGoalOptions(macro)}</select></label><label>Projeto atual<select name="project"><option value="">Selecionar meta...</option>${dashboardGoalOptions(project)}</select></label><label>Meta para hoje<select name="today"><option value="">Selecionar meta...</option>${dashboardGoalOptions(today)}</select></label><button class="primary-btn">Salvar foco</button></form><div class="upcoming-editor"><h3>Próximos desbloqueios</h3><div class="upcoming-edit-list">${(state.upcoming || []).map((name, index) => `<div class="upcoming-edit-item"><span>${index + 1}. ${escapeTodo(name)}</span><div><button class="ghost-btn" type="button" data-upcoming-up="${index}">↑</button><button class="ghost-btn" type="button" data-upcoming-down="${index}">↓</button><button class="ghost-btn danger" type="button" data-upcoming-remove="${index}">×</button></div></div>`).join('') || '<span class="muted">Nenhum desbloqueio selecionado.</span>'}</div><div class="upcoming-add"><select id="upcomingSelect"><option value="">Adicionar meta...</option>${state.goals.map(goal => `<option>${escapeTodo(goal.name)}</option>`).join('')}</select><button class="ghost-btn" id="addUpcoming" type="button">Adicionar</button></div></div></section>`; }
function bindDashboardEditor() { const form = document.querySelector('#dashboardFocusForm'); if (form) form.onsubmit = event => { event.preventDefault(); const data = new FormData(form); ['macro', 'project', 'today'].forEach(slot => { const goal = state.goals[Number(data.get(slot))]; if (goal) state[slot] = { ...goal }; }); save(); render(); }; document.querySelector('#closeDashboardEditor')?.addEventListener('click', () => document.querySelector('.dashboard-editor')?.remove()); document.querySelectorAll('[data-upcoming-up], [data-upcoming-down], [data-upcoming-remove]').forEach(button => button.onclick = () => { const index = Number(button.dataset.upcomingUp ?? button.dataset.upcomingDown ?? button.dataset.upcomingRemove); if (button.dataset.upcomingRemove !== undefined) state.upcoming.splice(index, 1); else { const direction = button.dataset.upcomingUp !== undefined ? -1 : 1, target = index + direction; if (target >= 0 && target < state.upcoming.length) [state.upcoming[index], state.upcoming[target]] = [state.upcoming[target], state.upcoming[index]]; } save(); render(); }); const addUpcoming = document.querySelector('#addUpcoming'); if (addUpcoming) addUpcoming.onclick = () => { const value = document.querySelector('#upcomingSelect').value; if (value && !state.upcoming.includes(value)) { state.upcoming.push(value); save(); render(); } }; }
function mountDashboard() { if (document.querySelector('#pageTitle')?.textContent !== 'Visão geral') return; const headers = [...document.querySelectorAll('.section-head')], focusHeader = headers.find(header => header.querySelector('h2')?.textContent === 'Seu foco agora'), upcomingHeader = headers.find(header => header.querySelector('h2')?.textContent === 'Próximos desbloqueios'); if (!focusHeader) return; const existingButtons = [...focusHeader.querySelectorAll('.dashboard-personalize')]; existingButtons.slice(1).forEach(button => button.remove()); const upcomingList = upcomingHeader?.nextElementSibling; if (upcomingList?.classList.contains('list')) upcomingList.innerHTML = (state.upcoming || []).map(name => `<div class="list-item"><div><h3>${escapeTodo(name)}</h3><p>Desbloqueio escolhido por você</p></div><span class="pill">Próximo</span></div>`).join('') || '<div class="empty">Nenhum desbloqueio selecionado.</div>'; if (!focusHeader.querySelector('.dashboard-personalize')) { const edit = document.createElement('button'); edit.className = 'ghost-btn dashboard-personalize'; edit.type = 'button'; edit.textContent = 'Personalizar'; focusHeader.appendChild(edit); edit.onclick = () => { document.querySelector('#content').insertAdjacentHTML('afterbegin', dashboardEditor()); bindDashboardEditor(); }; } else bindDashboardEditor(); }
new MutationObserver(() => mountDashboard()).observe(document.querySelector('#content'), { childList: true });
mountDashboard();
