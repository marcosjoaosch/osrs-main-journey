const pvmStages = ['Primeira tentativa', 'Primeiro KC', 'Invocation 150', 'Invocation 200', 'Invocation 250', 'Invocation 300', 'Expert consistente'];
const defaultBossCatalog = [
  ['barrows', 'Barrows', 'Minigame'], ['perilous-moons', 'Perilous Moons', 'Minigame'], ['vorkath', 'Vorkath', 'Bosses'], ['zulrah', 'Zulrah', 'Bosses'], ['tormented-demons', 'Tormented Demons', 'Bosses'], ['muspah', 'Muspah', 'Bosses'], ['gauntlet', 'Gauntlet', 'Minigame'], ['corrupted-gauntlet', 'Corrupted Gauntlet', 'Minigame'],
  ['scurrius', 'Scurrius', 'Bosses'], ['amoxliatl', 'Amoxliatl', 'Bosses'], ['deranged-archaeologist', 'Deranged Archaeologist', 'Wilderness'], ['chaos-fanatic', 'Chaos Fanatic', 'Wilderness'], ['crazy-archaeologist', 'Crazy Archaeologist', 'Wilderness'], ['callisto', 'Callisto', 'Wilderness'], ['calvarion', 'Calvarion', 'Wilderness'], ['chaos-elemental', 'Chaos Elemental', 'Wilderness'], ['venenatis', 'Venenatis', 'Wilderness'], ['vetion', 'Vet’ion', 'Wilderness'], ['spindel', 'Spindel', 'Wilderness'], ['artio', 'Artio', 'Wilderness'],
  ['general-graardor', 'General Graardor', 'God Wars'], ['commander-zilyana', 'Commander Zilyana', 'God Wars'], ['kreearra', 'Kree’arra', 'God Wars'], ['kril-tsutsaroth', 'K’ril Tsutsaroth', 'God Wars'], ['nex', 'Nex', 'God Wars'], ['corporeal-beast', 'Corporeal Beast', 'Bosses'], ['alchemical-hydra', 'Alchemical Hydra', 'Slayer'], ['cerberus', 'Cerberus', 'Slayer'], ['kraken', 'Kraken', 'Slayer'], ['thermonuclear-smoke-devil', 'Thermonuclear smoke devil', 'Slayer'], ['abyssal-sire', 'Abyssal Sire', 'Slayer'], ['grotesque-guardians', 'Grotesque Guardians', 'Slayer'], ['nightmare', 'The Nightmare', 'Bosses'], ['phosanis-nightmare', 'Phosani’s Nightmare', 'Bosses'], ['tombs-of-amascut', 'Tombs of Amascut', 'Raids'], ['chambers-of-xeric', 'Chambers of Xeric', 'Raids'], ['theatre-of-blood', 'Theatre of Blood', 'Raids'],
  ['duke-sucellus', 'Duke Sucellus', 'Desert Treasure II'], ['the-whisperer', 'The Whisperer', 'Desert Treasure II'], ['vardorvis', 'Vardorvis', 'Desert Treasure II'], ['leviathan', 'The Leviathan', 'Desert Treasure II'], ['tempoross', 'Tempoross', 'Skilling'], ['wintertodt', 'Wintertodt', 'Skilling'], ['zalcano', 'Zalcano', 'Skilling'], ['yama', 'Yama', 'Bosses'], ['araxxor', 'Araxxor', 'Slayer'], ['royal-titans', 'Royal Titans', 'Bosses'], ['sarachnis', 'Sarachnis', 'Bosses'], ['hespori', 'Hespori', 'Skilling'], ['obor', 'Obor', 'Free-to-play'], ['bryophyta', 'Bryophyta', 'Free-to-play'], ['giant-mole', 'Giant Mole', 'Bosses'], ['king-black-dragon', 'King Black Dragon', 'Bosses'], ['dagannoth-rex', 'Dagannoth Rex', 'Dagannoth'], ['dagannoth-prime', 'Dagannoth Prime', 'Dagannoth'], ['dagannoth-supreme', 'Dagannoth Supreme', 'Dagannoth'], ['dusk', 'Dusk', 'Slayer']
].filter((item, index, list) => list.findIndex(other => other[0] === item[0]) === index);
const defaultRaidCatalog = [['toa', 'Tombs of Amascut', 'Desert'], ['cox', 'Chambers of Xeric', 'Kourend'], ['tob', 'Theatre of Blood', 'Morytania']];

function pvmDetail(name) { if (!state.pvmDetails) state.pvmDetails = {}; if (!state.pvmDetails[name]) state.pvmDetails[name] = { target: 25, best: '', state: 'Nunca tentei', liked: false, notes: '', gear: '', project: false }; return state.pvmDetails[name]; }
function raidDetail(id) { if (!state.raidDetails) state.raidDetails = {}; if (!state.raidDetails[id]) state.raidDetails[id] = { invocation: 0, target: id === 'toa' ? 150 : 0, best: '', notes: '', state: 'Nunca tentei' }; return state.raidDetails[id]; }
function raidCatalog() { if (!state.raidCatalog?.length) state.raidCatalog = defaultRaidCatalog.map(([id, name, region]) => ({ id, name, region, wiki: name })); return state.raidCatalog; }
function raidCard(raid) { const detail = raidDetail(raid.id), hasInvocation = raid.id === 'toa', progress = hasInvocation ? Math.min(100, Math.round((detail.invocation / Math.max(1, detail.target || 150)) * 100)) : (detail.state === 'Dominada' ? 100 : detail.state === 'Consistente' ? 75 : detail.state === 'Primeiro KC' ? 25 : 0); return `<article class="raid-card"><button class="raid-card-main" type="button" data-raid-edit="${raid.id}"><div class="raid-icon">${raid.id === 'toa' ? '𓂀' : raid.id === 'cox' ? '✦' : raid.id === 'tob' ? '☠' : '⚔'}</div><div><span class="eyebrow">${escapeTodo(raid.region)} · ${escapeTodo(detail.state)}</span><h3>${escapeTodo(raid.name)}</h3><p>${hasInvocation ? `Invocation ${detail.invocation} · meta ${detail.target}` : 'Progressão por domínio e consistência'}</p><div class="progress"><i style="width:${progress}%"></i></div></div><span class="pill">${progress}%</span></button><div class="raid-quick"><button class="ghost-btn" type="button" data-raid-invocation="${raid.id}">${hasInvocation ? '+ Invocation' : '+ progresso'}</button><button class="ghost-btn" type="button" data-raid-edit="${raid.id}">Editar</button></div></article>`; }
function raidEditor(id) { const raid = raidCatalog().find(item => item.id === id), detail = raidDetail(id), hasInvocation = id === 'toa'; return `<section class="card raid-editor"><div class="section-head" style="margin-top:0"><div><span class="eyebrow">EDITAR RAID</span><h2>${escapeTodo(raid.name)}</h2></div><button class="ghost-btn" id="closeRaidEditor" type="button">Fechar</button></div><form class="form" id="raidForm"><label>Estado<select name="state">${['Nunca tentei','Experimentando','Primeiro KC','Consistente','Dominada','Projeto de completion'].map(option => `<option ${option === detail.state ? 'selected' : ''}>${option}</option>`).join('')}</select></label>${hasInvocation ? `<label>Invocation atual<input name="invocation" type="number" min="0" value="${detail.invocation}"></label><label>Meta de Invocation<input name="target" type="number" min="1" value="${detail.target}"></label>` : ''}<label>Melhor marca<input name="best" value="${escapeTodo(detail.best)}" placeholder="Ex: maior invocation, tempo, purple..."></label><label>Observações<textarea name="notes" placeholder="Aprendizados, composição, estratégia...">${escapeTodo(detail.notes)}</textarea></label><div class="form-actions"><button class="ghost-btn danger" id="removeRaid" type="button">Remover da seleção</button><button class="primary-btn">Salvar raid</button></div></form></section>`; }
function raidManager() { const selected = state.raidVisible || []; return `<section class="card raid-manager"><div class="section-head" style="margin-top:0"><div><span class="eyebrow">CONFIGURAR RAIDS</span><h2>Escolha as raids visíveis</h2><p class="muted">Selecione o que você quer acompanhar no painel de domínio.</p></div><span class="pill">${selected.length} selecionadas</span></div><div class="raid-select-list">${raidCatalog().map(raid => `<label class="raid-select-item"><input type="checkbox" data-raid-visible="${raid.id}" ${selected.includes(raid.id) ? 'checked' : ''}><span><strong>${escapeTodo(raid.name)}</strong><small>${escapeTodo(raid.region)}${raid.id === 'toa' ? ' · invocations' : ''}</small></span></label>`).join('')}</div></section>`; }
function renderRaids() { if (!Array.isArray(state.raidVisible)) state.raidVisible = ['toa']; const raids = raidCatalog().filter(raid => state.raidVisible.includes(raid.id)); return `<section class="card raids-panel"><div class="section-head" style="margin-top:0"><div><span class="eyebrow">RAIDS E INVOCATIONS</span><h2>Campanha de domínio</h2><p class="muted">Cada raid tem seu próprio progresso e anotações.</p></div><button class="ghost-btn" id="toggleRaidManager" type="button">⚙ Configurar raids</button></div><div id="raidManagerSlot"></div><div class="raid-grid">${raids.map(raidCard).join('') || '<div class="empty">Nenhuma raid selecionada.</div>'}</div></section>`; }
function pvmCatalog() { if (!state.pvmCatalog?.length) state.pvmCatalog = defaultBossCatalog.map(([id, name, category]) => ({ id, name, category, wiki: name })); const existing = new Map(state.pvmCatalog.map(item => [item.id, item])); defaultBossCatalog.forEach(([id, name, category]) => { if (!existing.has(id)) state.pvmCatalog.push({ id, name, category, wiki: name }); }); return state.pvmCatalog; }
function pvmInit() { const catalog = pvmCatalog(); if (!Array.isArray(state.pvmVisible)) state.pvmVisible = state.bosses.map(boss => catalog.find(item => item.name === boss[0])?.id).filter(Boolean); state.pvmVisible = state.pvmVisible.filter(id => catalog.some(item => item.id === id)); state.bosses = state.pvmVisible.map(id => { const item = catalog.find(entry => entry.id === id); const old = state.bosses.find(boss => boss[0] === item.name); return old || [item.name, 0, 'Nunca tentei']; }); }
function pvmImage(item, detail = {}) { return detail.image || item.image || `https://oldschool.runescape.wiki/w/Special:FilePath/${encodeURIComponent(item.name.replace(/’/g, "'"))}.png`; }
function pvmCard(boss, index) { const item = pvmCatalog().find(entry => entry.name === boss[0]) || { id: boss[0], name: boss[0], category: 'Personalizado' }; const detail = pvmDetail(boss[0]), progress = Math.min(100, Math.round((Number(boss[1]) / Math.max(1, Number(detail.target) || 25)) * 100)); return `<article class="pvm-card ${detail.project ? 'pvm-project' : ''}"><button class="pvm-card-main" type="button" data-pvm-edit="${index}"><div class="pvm-boss-visual"><img src="${pvmImage(item, detail)}" alt="${escapeTodo(item.name)}" loading="lazy" onerror="this.style.display='none';this.nextElementSibling.style.display='grid'"><span class="pvm-boss-fallback">${escapeTodo(item.name.slice(0, 2).toUpperCase())}</span></div><div class="pvm-card-copy"><span class="eyebrow">${escapeTodo(item.category)} · ${escapeTodo(detail.state)}</span><h3>${escapeTodo(boss[0])}</h3><p>${boss[1]} KC · meta ${detail.target} KC${detail.liked ? ' · ♥ gostei' : ''}</p><div class="progress"><i style="width:${progress}%"></i></div></div><span class="pill">${progress}%</span></button><div class="pvm-quick"><button class="ghost-btn" type="button" data-pvm-kc="${index}">+ KC</button><button class="ghost-btn" type="button" data-pvm-edit="${index}">Detalhes</button></div></article>`; }
function pvmEditor(index) { const boss = state.bosses[index], detail = pvmDetail(boss[0]); return `<section class="card pvm-editor"><div class="section-head" style="margin-top:0"><div><span class="eyebrow">EDITAR PASSAPORTE</span><h2>${escapeTodo(boss[0])}</h2></div><button class="ghost-btn" id="closePvmEditor" type="button">Fechar</button></div><form class="form" id="pvmForm"><label>Imagem do boss<input name="imageFile" type="file" accept="image/png,image/jpeg,image/webp"><small class="muted">PNG transparente recomendado. Se escolher uma imagem, ela substitui a imagem da Wiki neste boss.</small></label>${detail.image ? `<div class="pvm-upload-preview"><img src="${detail.image}" alt="Imagem atual"><button class="ghost-btn" id="removePvmImage" type="button">Remover imagem manual</button></div>` : ''}<label>KC atual<input name="kc" type="number" min="0" value="${boss[1]}"></label><label>Meta de KC<input name="target" type="number" min="1" value="${detail.target}"></label><label>Estado de aprendizado<select name="state">${['Nunca tentei','Experimentando','Primeiro KC','25 KC','50 KC','Consistente','Dominado','Projeto de greenlog'].map(option => `<option ${option === detail.state ? 'selected' : ''}>${option}</option>`).join('')}</select></label><label>Melhor marca<input name="best" value="${escapeTodo(detail.best)}" placeholder="Ex: tempo ou maior invocation"></label><label>Gear recomendado<textarea name="gear" placeholder="Setup, inventário e gear...">${escapeTodo(detail.gear)}</textarea></label><label>Observações<textarea name="notes" placeholder="O que aprendi? O que testar depois?">${escapeTodo(detail.notes)}</textarea></label><label class="check-line"><input name="liked" type="checkbox" ${detail.liked ? 'checked' : ''}> Eu gosto desse conteúdo</label><label class="check-line"><input name="project" type="checkbox" ${detail.project ? 'checked' : ''}> Projeto de greenlog</label><div class="form-actions"><button class="ghost-btn danger" id="deletePvm" type="button">Remover da seleção</button><button class="primary-btn">Salvar boss</button></div></form><div class="pvm-editor-actions"><button class="ghost-btn" id="usePvmProject" type="button">Usar como projeto atual</button><button class="ghost-btn" id="usePvmToday" type="button">Usar na sessão de hoje</button></div></section>`; }
function pvmManager() { const catalog = pvmCatalog(); return `<section class="card pvm-manager"><div class="section-head" style="margin-top:0"><div><span class="eyebrow">CONFIGURAR PAINEL</span><h2>Escolha os bosses visíveis</h2><p class="muted">O catálogo completo fica aqui; marque apenas o que quer acompanhar no Passaporte.</p></div><span class="pill">${state.pvmVisible.length} selecionados</span></div><div class="pvm-manager-tools"><input id="pvmSearch" placeholder="Pesquisar boss..." autocomplete="off"><select id="pvmCategory"><option value="">Todas as categorias</option>${[...new Set(catalog.map(item => item.category))].sort().map(category => `<option>${escapeTodo(category)}</option>`).join('')}</select><button class="ghost-btn" id="selectAllPvm" type="button">Selecionar todos</button><button class="ghost-btn" id="clearAllPvm" type="button">Limpar seleção</button></div><div class="pvm-catalog-list">${catalog.map(item => `<label class="pvm-catalog-item" data-pvm-catalog data-name="${escapeTodo(item.name.toLowerCase())}" data-category="${escapeTodo(item.category)}"><input type="checkbox" data-pvm-visible="${item.id}" ${state.pvmVisible.includes(item.id) ? 'checked' : ''}><span class="pvm-mini-icon"><img src="${pvmImage(item)}" alt="" loading="lazy" onerror="this.style.display='none'"></span><span><strong>${escapeTodo(item.name)}</strong><small>${escapeTodo(item.category)}</small></span></label>`).join('')}</div><form class="pvm-add-custom" id="pvmCustomForm"><input name="name" required placeholder="Adicionar conteúdo personalizado..."><select name="category"><option>Personalizado</option><option>Bosses</option><option>Raids</option><option>Minigame</option></select><button class="primary-btn">Adicionar e mostrar</button></form><p class="muted pvm-source-note">Imagens carregadas da OSRS Wiki quando disponíveis. Elas são usadas apenas como referência visual no seu painel local.</p></section>`; }
function renderPvm() { pvmInit(); const cards = state.bosses.map((boss, index) => pvmCard(boss, index)).join(''); return `<div class="section-head"><div><span class="eyebrow">APRENDIZADO E DOMÍNIO</span><h1>Passaporte PvM</h1></div><button class="primary-btn" id="togglePvmManager">⚙ Gerenciar bosses</button></div><p class="muted">Acompanhe KC, aprendizado, equipamentos e projetos. O que aparece aqui é decidido no catálogo abaixo.</p><div id="pvmManagerSlot"></div><div class="pvm-layout"><section class="card"><div class="section-head" style="margin-top:0"><h2>Bosses selecionados</h2><span class="pill">${state.bosses.filter(b => b[1] > 0).length}/${state.bosses.length} experimentados</span></div><div class="pvm-grid">${cards || '<div class="empty">Nenhum boss selecionado. Abra “Gerenciar bosses” para escolher.</div>'}</div></section><div>${renderRaids()}</div></div>`; }
function openPvmEditor(index) { const content = document.querySelector('#content'); document.querySelector('.pvm-editor')?.remove(); content.insertAdjacentHTML('afterbegin', pvmEditor(index)); const form = document.querySelector('#pvmForm'), boss = state.bosses[index], detail = pvmDetail(boss[0]); form.onsubmit = event => { event.preventDefault(); const data = new FormData(form); const file = form.elements.imageFile.files[0]; const finish = image => { boss[1] = Number(data.get('kc')) || 0; Object.assign(detail, { target: Number(data.get('target')) || 25, state: data.get('state'), best: data.get('best'), gear: data.get('gear'), notes: data.get('notes'), liked: data.get('liked') === 'on', project: data.get('project') === 'on' }); if (image) detail.image = image; save(); render(); }; if (file) { const reader = new FileReader(); reader.onload = () => finish(reader.result); reader.readAsDataURL(file); } else finish(''); }; document.querySelector('#closePvmEditor').onclick = () => document.querySelector('.pvm-editor')?.remove(); document.querySelector('#removePvmImage')?.addEventListener('click', () => { delete detail.image; save(); render(); }); document.querySelector('#deletePvm').onclick = () => { state.pvmVisible = state.pvmVisible.filter(id => id !== pvmCatalog().find(item => item.name === boss[0])?.id); save(); render(); }; document.querySelector('#usePvmProject').onclick = () => { state.project = { name: boss[0], desc: 'Boss em aprendizado no Passaporte PvM.', progress: 0 }; save(); toast('Boss definido como projeto atual'); }; document.querySelector('#usePvmToday').onclick = () => { todos.push({ text: `Fazer uma trip de ${boss[0]}`, done: false }); saveTodos(); toast('Boss adicionado à sessão de hoje'); }; }
function filterPvmCatalog() { const query = (document.querySelector('#pvmSearch')?.value || '').toLowerCase(), category = document.querySelector('#pvmCategory')?.value || ''; document.querySelectorAll('[data-pvm-catalog]').forEach(item => { item.hidden = !item.dataset.name.includes(query) || (category && item.dataset.category !== category); }); }
function openRaidEditor(id) { const content = document.querySelector('#content'); document.querySelector('.raid-editor')?.remove(); content.insertAdjacentHTML('afterbegin', raidEditor(id)); const form = document.querySelector('#raidForm'), detail = raidDetail(id), raid = raidCatalog().find(item => item.id === id); form.onsubmit = event => { event.preventDefault(); const data = new FormData(form); Object.assign(detail, { state: data.get('state'), best: data.get('best'), notes: data.get('notes') }); if (id === 'toa') { detail.invocation = Number(data.get('invocation')) || 0; detail.target = Number(data.get('target')) || 150; } save(); render(); }; document.querySelector('#closeRaidEditor').onclick = () => document.querySelector('.raid-editor')?.remove(); document.querySelector('#removeRaid').onclick = () => { state.raidVisible = (state.raidVisible || []).filter(item => item !== id); save(); render(); }; }
function bindRaids() { document.querySelectorAll('[data-raid-edit]').forEach(button => button.onclick = () => openRaidEditor(button.dataset.raidEdit)); document.querySelectorAll('[data-raid-invocation]').forEach(button => button.onclick = () => { const detail = raidDetail(button.dataset.raidInvocation), id = button.dataset.raidInvocation; if (id === 'toa') detail.invocation += 10; else detail.state = detail.state === 'Nunca tentei' ? 'Primeiro KC' : detail.state === 'Primeiro KC' ? 'Consistente' : 'Dominada'; save(); render(); }); document.querySelector('#toggleRaidManager')?.addEventListener('click', () => { const slot = document.querySelector('#raidManagerSlot'); slot.innerHTML = slot.innerHTML ? '' : raidManager(); bindRaids(); }); document.querySelectorAll('[data-raid-visible]').forEach(input => input.onchange = () => { state.raidVisible = state.raidVisible || []; const id = input.dataset.raidVisible; if (input.checked && !state.raidVisible.includes(id)) state.raidVisible.push(id); if (!input.checked) state.raidVisible = state.raidVisible.filter(item => item !== id); save(); render(); }); }
function bindPvm() { document.querySelectorAll('[data-pvm-edit]').forEach(button => button.onclick = event => { event.preventDefault(); event.stopPropagation(); openPvmEditor(Number(button.dataset.pvmEdit)); }); document.querySelectorAll('[data-pvm-kc]').forEach(button => button.onclick = event => { event.preventDefault(); event.stopPropagation(); const boss = state.bosses[Number(button.dataset.pvmKc)]; boss[1] = Number(boss[1]) + 1; const detail = pvmDetail(boss[0]); if (boss[1] >= 50) detail.state = 'Consistente'; else if (boss[1] >= 25) detail.state = '25 KC'; else if (boss[1] >= 1) detail.state = 'Primeiro KC'; save(); render(); }); document.querySelectorAll('[data-toa-stage]').forEach(input => input.onchange = () => { if (!state.toaStages) state.toaStages = []; state.toaStages[Number(input.dataset.toaStage)] = input.checked; save(); }); document.querySelector('#saveToa')?.addEventListener('click', () => { state.toaNote = document.querySelector('#toaNote').value; save(); }); document.querySelector('#togglePvmManager')?.addEventListener('click', () => { const slot = document.querySelector('#pvmManagerSlot'); slot.innerHTML = slot.innerHTML ? '' : pvmManager(); bindPvm(); }); document.querySelectorAll('[data-pvm-visible]').forEach(input => input.onchange = () => { const id = input.dataset.pvmVisible; state.pvmVisible = state.pvmVisible || []; if (input.checked && !state.pvmVisible.includes(id)) state.pvmVisible.push(id); if (!input.checked) state.pvmVisible = state.pvmVisible.filter(item => item !== id); save(); render(); }); document.querySelector('#pvmSearch')?.addEventListener('input', filterPvmCatalog); document.querySelector('#pvmCategory')?.addEventListener('change', filterPvmCatalog); document.querySelector('#selectAllPvm')?.addEventListener('click', () => { state.pvmVisible = pvmCatalog().map(item => item.id); save(); render(); }); document.querySelector('#clearAllPvm')?.addEventListener('click', () => { state.pvmVisible = []; save(); render(); }); document.querySelector('#pvmCustomForm')?.addEventListener('submit', event => { event.preventDefault(); const data = new FormData(event.target), name = data.get('name').trim(), id = `custom-${Date.now()}`; state.pvmCatalog = pvmCatalog(); state.pvmCatalog.push({ id, name, category: data.get('category'), wiki: name }); state.pvmVisible = [...(state.pvmVisible || []), id]; state.bosses.push([name, 0, 'Nunca tentei']); pvmDetail(name); save(); render(); }); }
function loadBossCatalog() { if (window.pvmCatalogLoading || state.pvmCatalogLoaded) return; window.pvmCatalogLoading = true; fetch('https://oldschool.runescape.wiki/api.php?action=query&format=json&origin=*&list=categorymembers&cmtitle=Category%3ABosses&cmlimit=max&cmtype=page').then(response => response.json()).then(data => { const catalog = pvmCatalog(), known = new Set(catalog.map(item => item.wiki)); (data.query?.categorymembers || []).forEach(entry => { if (!known.has(entry.title)) { const id = `wiki-${entry.pageid}`; catalog.push({ id, name: entry.title, category: 'Catálogo Wiki', wiki: entry.title }); known.add(entry.title); } }); state.pvmCatalogLoaded = true; save(); if (document.querySelector('#pageTitle')?.textContent === 'Passaporte PvM') render(); }).catch(() => {}).finally(() => { window.pvmCatalogLoading = false; }); }
function loadPvmImages() { const catalog = pvmCatalog(), selected = new Set(state.pvmVisible || []), missing = catalog.filter(item => selected.has(item.id) && !item.image && item.wiki); if (!missing.length || window.pvmImagesLoading) return; window.pvmImagesLoading = true; const titles = missing.map(item => item.wiki).join('|'); fetch(`https://oldschool.runescape.wiki/api.php?action=query&format=json&origin=*&prop=pageimages&piprop=original&titles=${encodeURIComponent(titles)}`).then(response => response.json()).then(data => { Object.values(data.query?.pages || {}).forEach(page => { const item = catalog.find(entry => entry.wiki === page.title); if (item && page.original?.source) item.image = page.original.source; }); save(); if (document.querySelector('#pageTitle')?.textContent === 'Passaporte PvM') render(); }).catch(() => {}).finally(() => { window.pvmImagesLoading = false; }); }
function mountPvm() { if (document.querySelector('#pageTitle')?.textContent !== 'Passaporte PvM') return; const content = document.querySelector('#content'); if (!content.dataset.pvmMounted || !document.querySelector('.pvm-layout')) { content.innerHTML = renderPvm(); content.dataset.pvmMounted = 'true'; } loadBossCatalog(); loadPvmImages(); bindPvm(); bindRaids(); }
new MutationObserver(() => mountPvm()).observe(document.querySelector('#content'), { childList: true });
mountPvm();
